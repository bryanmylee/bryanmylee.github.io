<script lang="ts">
  import IntroProjectItem from './IntroProjectItem.svelte';
  import type { IProject } from '@my/models/ProjectsPage';

  export { className as class };
  let className = '';
  export let projects: Record<string, IProject> = {};
</script>

<div class={className}>
  <div class="prose">
    <h2 class="font-mono font-bold">Projects</h2>
  </div>
  <div class="flex items-stretch mt-3 overflow-x-auto space-x-3">
    {#each Object.entries(projects) as [key, { projectName, tech, logo }] (key)}
      <IntroProjectItem
        {key}
        logoSrc={logo}
        title={projectName}
        {tech}
        />
    {/each}
  </div>
</div>

