<script lang="ts">
  import IntroExperienceItem from './IntroExperienceItem.svelte';
  import type { IExperience } from '@my/models/ExperiencePage';

  export { className as class };
  let className = '';
  export let experiences: Record<string, IExperience> = {};
</script>

<div class={className}>
  <h2 class="font-bold text-accent">Experience</h2>
  <div class="flex items-stretch mt-3 overflow-x-auto space-x-3">
    {#each Object.entries(experiences) as [key, { companyName, position, logo }] (key)}
      <IntroExperienceItem {key} {companyName} {position} {logo}/>
    {/each}
  </div>
</div>

