<script lang="ts">
  import Logo from '@my/components/Logo';
  import type Experience from '@my/models/Experience';

  export let experience: Record<string, Experience> = {};
  export let selectedKey: string;
</script>

<div class="flex flex-col items-center space-y-3 w-32">
  {#each Object.entries(experience) as [key, { companyName, logo }] (key)}
    <button
      class={`transition-all ${selectedKey === key ? 'w-32' : 'w-16'}`}
      on:click={() => selectedKey = key}
      >
      <Logo src={logo} alt={companyName}/>
    </button>
  {/each}
</div>

