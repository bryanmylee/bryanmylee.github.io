<script lang="ts">
  import Logo from '@my/components/Logo';
  import type { IExperience } from '@my/models/ExperiencePage';

  export let experiences: Record<string, IExperience> = {};
  export let selectedKey: string;
</script>

<div class="
  flex items-center space-x-3 min-h-24
  lg:flex-col lg:space-x-0 lg:space-y-3 lg:min-w-24"
  >
  {#each Object.entries(experiences) as [key, { companyName, logo }] (key)}
    <button
      class={`transition-all ${selectedKey === key ? 'w-24' : 'w-16'}`}
      on:click={() => selectedKey = key}
      >
      <Logo src={logo} alt={companyName}/>
    </button>
  {/each}
</div>

