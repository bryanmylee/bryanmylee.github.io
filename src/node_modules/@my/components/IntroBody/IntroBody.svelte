<script lang="ts">
  import { fade } from 'svelte/transition';
  import profilePhoto from '@my/assets/profile.jpg';

  export { className as class };
  let className = '';
  export let paragraphs: string[] = [];
  export let quote: string;
</script>

<div class={`${className}
  flex flex-col space-y-3
  md:flex-row md:space-x-3 md:space-y-0`}
  >
  <div class="space-y-3">
    {#each paragraphs as paragraph}
      <p class="p-3 card prose leading-8">
        {@html paragraph}
      </p>
    {/each}
    <p class="
      relative card p-3 pl-6 leading-8 prose italic
      before:empty-content before:absolute before:bg-red
      before:w-3 before:left-0 before:top-0 before:bottom-0"
      >
      {@html quote}
    </p>
  </div>
  <img src={profilePhoto} alt="profile" in:fade class="md:max-h-96">
</div>

